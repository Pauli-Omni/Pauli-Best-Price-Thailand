Flutter-Bundle-Kopie der Avatar-Loops.

Quelle: public/assets/avatar/ (wird von scripts/build-store-artifacts.sh synchronisiert).
WebView lädt die Live-PWA; diese Assets dienen als Offline-Fallback in nativen Shells.
